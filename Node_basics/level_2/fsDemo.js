const fs = require("fs"); // now whatever we do they are callback based syntax
// for input output
//  fs bidefault shifts with nodejs

const fsPro = require("fs/promises");
// they all are promised based syntax